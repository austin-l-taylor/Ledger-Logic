<!-- entry_details.html -->
{% extends "main_page/base.html" %}
{% load humanize %}

{% block content %}
<div class="container mt-4">
    <a href="{% url 'ledger' account.id %}" class="btn btn-secondary mb-3" title="go back">Back to Ledger Detail</a>
    <div class="row">
        <h2 class="mb-4 text-center">Journal Entry Details</h2>
    </div>

    <div class="table-responsive">
        <table class="table table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Date</th>
                    <th>Account</th>
                    <th>Debit</th>
                    <th>Credit</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ entry.date }}</td>
                <td>{{ entry.account.account_name }}</td>
                <td>{{ entry.debit|intcomma }}</td>
                <td>{{ entry.credit|intcomma }}</td>
                <td>{{ entry.status }}</td>
                <td>
                    {% if is_admin %}
                        {% if entry.status == 'Pending' %}
                            <!-- Approve Button -->
                            <form action="{% url 'journal_entry_page' %}" method="post" style="display: inline-block;">
                                {% csrf_token %}
                                <input type="hidden" name="entry_id" value="{{ entry.id }}">
                                <button type="submit" name="approve" class="btn btn-sm btn-success">Approve</button>
                            </form>
        
                            <!-- Reject Button -->
                            <form action="{% url 'journal_entry_page' %}" method="post" style="display: inline-block;">
                                {% csrf_token %}
                                <input type="hidden" name="entry_id" value="{{ entry.id }}">
                                <button type="submit" name="reject" class="btn btn-sm btn-danger">Reject</button>
                            </form>
                        {% elif entry.status == 'Approved' %}
                            Approved
                        {% elif entry.status == 'Rejected' %}
                            Rejected
                        {% endif %}
                    {% endif %}
                </td>
            </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
